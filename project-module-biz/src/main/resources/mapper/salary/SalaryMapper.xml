<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lz.module.biz.dal.mysql.salary.SalaryMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="getTotalPayableAmount" resultType="java.math.BigDecimal">
        select sum(payable_amount) from biz_salary
        <where>
            and deleted = false
            <if test="workerName != null and workerName != ''">
                and worker_name like concat('%', #{workerName}, '%')
            </if>
            <if test="isSettlement != null and isSettlement != ''">
                and is_settlement = #{isSettlement}
            </if>
            <if test="settlementTime != null and settlementTime.length == 2">
                and settlement_time between
                #{settlementTime[0], javaType=java.time.LocalDateTime, jdbcType=TIMESTAMP}
                and
                #{settlementTime[1], javaType=java.time.LocalDateTime, jdbcType=TIMESTAMP}
            </if>
            <if test="createTime != null and createTime.length == 2">
                AND create_time BETWEEN #{createTime[0], javaType=java.time.LocalDateTime, jdbcType=TIMESTAMP} AND
                #{createTime[1], javaType=java.time.LocalDateTime, jdbcType=TIMESTAMP}
            </if>
        </where>
    </select>
</mapper>
